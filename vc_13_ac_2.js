let email = prompt("Enter your email", "");
if (email.length < 3) {
    alert("Not an email");
}
if (email.length > 3) {
    alert("Message has been sent to the email");
}

/* let email = prompt("Enter your email", "");
 if (email.length < 3) {
     alert("Not an email"); }
     else
         alert( "Message has been sent to the email"); */

